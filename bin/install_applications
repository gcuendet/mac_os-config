#! /usr/bin/env bash

# Installs applications.

install_dmg_app "$DOCKER_APP_URL" "$DOCKER_VOLUME_NAME" "$DOCKER_APP_NAME"
install_program "$GIT_FILTER_REPO_APP_URL" "$GIT_FILTER_REPO_APP_NAME"
install_dmg_app "$HAND_BRAKE_CLI_APP_URL" "$HAND_BRAKE_CLI_VOLUME_NAME" "$HAND_BRAKE_CLI_APP_NAME"
install_zip_app "$SUBLIME_URL_HANDLER_APP_URL" "$SUBLIME_URL_HANDLER_APP_NAME"
